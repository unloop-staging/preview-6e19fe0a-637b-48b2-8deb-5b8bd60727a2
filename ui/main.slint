// Generated by Unloop - 2026-01-01T17:48:55.287Z
// Smart Washing Machine: Advanced washing machine control with cycle management and monitoring

import { MaterialWindow } from "@material";
import { AppData } from "data.slint";
import { HomeScreen } from "screens.slint";
import { ControlScreen } from "screens.slint";
import { SettingsScreen } from "screens.slint";

export component App inherits MaterialWindow {
    title: "Smart Washing Machine";
    width: 800px;
    height: 480px;
    background: #1E1E1E;
    
    in-out property <string> active-screen: "home";
    in-out property <AppData> data: {
        time-remaining: 0,
        load-weight: 0,
        water-temp: 20,
        door-status: "closed",
        water-status: "ok",
        drain-status: "ok",
        spin-speed: 0,
        target-spin-speed: 800,
        default-cycle: "Cotton",
        water-temp-pref: "Warm",
        auto-detergent: true,
        cycle-complete-beep: true,
        door-open-alert: true,
        maintenance-reminders: true,
        brightness: 80,
        sleep-timer: 10,
        model-number: "SW-5000",
        firmware-version: "2.1.4",

    };
    
    callback start-cycle-clicked();
    callback pause-clicked();
    callback add-time-clicked();
    callback target-changed(float);
    callback mode-changed(string);
    
    VerticalLayout {
        Rectangle {
            vertical-stretch: 1;
            
            if active-screen == "home" : HomeScreen {
                data <=> root.data;
            }

            if active-screen == "control" : ControlScreen {
                data <=> root.data;
            }

            if active-screen == "settings" : SettingsScreen {
                data <=> root.data;
            }

        }
        
        Rectangle {
            height: 80px;
            background: #2A2A2A;
            
            HorizontalLayout {
                alignment: space-evenly;
                
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "home"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Home";
                            font-size: 12px;
                            color: active-screen == "home" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "control"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Control";
                            font-size: 12px;
                            color: active-screen == "control" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "settings"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Settings";
                            font-size: 12px;
                            color: active-screen == "settings" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
            }
        }
    }
}
